{
  "name": "elasticsearch-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "predev": "rm -rf ./elasticsearch/plugins/*",
    "start": "node server.js",
    "dev": "docker-compose up --build",
    "stop": "docker-compose down"
  }
}
