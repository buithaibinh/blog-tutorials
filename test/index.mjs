import { fromUrl, fromBufferWithMimeType, fromBuffer } from '@sk-global/text-extractor';

import {generateDescriptionFromDom} from '@sk-global/scrapeer'

(async () => {
  console.log('Hello World!');
  generateDescriptionFromDom()
})();
